mutation setRating($subject: HashID!, $subjectType: RatingSubjectType!, $rating: RatingInput!) {
    setRating(subject: $subject, subjectType: $subjectType, rating: $rating) {
        __typename
        errors {
            __typename
            argument
            code
            message
        }
    }
}