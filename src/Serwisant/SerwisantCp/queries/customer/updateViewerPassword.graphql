mutation updateViewerPassword($currentPassword: String!, $password: String!, $passwordConfirmation: String!) {
    updateViewerPassword(currentPassword: $currentPassword, password: $password, passwordConfirmation: $passwordConfirmation) {
        __typename
        errors {
            __typename
            argument
            message
            code
        }
    }
}