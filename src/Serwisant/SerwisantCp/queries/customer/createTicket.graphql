mutation createTicket($ticket: TicketInput!, $temporaryFiles: [HashID!], $devices: [HashID!]) {
    createTicket(ticket: $ticket, temporaryFiles: $temporaryFiles, devices: $devices) {
        __typename
        errors {
            __typename
            argument
            code
        }
    }
}