mutation createTicket($ticket: TicketInput!, $temporaryFiles: [HashID!]) {
    createTicket(ticket: $ticket, temporaryFiles: $temporaryFiles) {
        __typename
        errors {
            __typename
            argument
            code
        }
    }
}