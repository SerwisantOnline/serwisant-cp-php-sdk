mutation createTicket($ticket: TicketInput!, $temporaryFiles: [HashID!], $devices: [HashID!], $address: AddressInput) {
    createTicket(ticket: $ticket, temporaryFiles: $temporaryFiles, devices: $devices, address: $address) {
        __typename
        errors {
            __typename
            argument
            code
        }
    }
}