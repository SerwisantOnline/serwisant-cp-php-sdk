mutation SetPassword($password: String!, $passwordConfirmation: String!, $resetToken: String!) {
    setPassword(password: $password, passwordConfirmation: $passwordConfirmation, resetToken: $resetToken) {
        __typename
        errors {
            __typename
            argument
            code
            message
        }
    }
}