query {
    customerCustomFields: customFields(form: CUSTOMER) {
        __typename
        ID
        name
        description
        type
        selectOptions
        required
    }
    customerAgreements: customerAgreements {
        __typename
        ID
        title
        content
        visiblePersonal
        visibleBusiness
    }
}
