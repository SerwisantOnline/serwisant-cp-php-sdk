mutation ResetPassword($loginOrEmail: String!, $subject: PasswordResetSubject!) {
    resetPassword(loginOrEmail: $loginOrEmail, subject: $subject) {
        __typename
        errors {
            __typename
            argument
            code
            message
        }
    }
}